#pragma once

#include "Wigner/Base/Base.hpp"
#include <string>

namespace Wigner
{
    class Texture2D
    {
    public:
        Texture2D(std::string& path);
        ~Texture2D();
    private:
    
    };
}